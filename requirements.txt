# Runtime dependencies (training + serving the API).
# Versions pinned for reproducibility. After you confirm everything runs
# in your environment, regenerate exact pins with:  pip freeze > requirements.txt
streamlit==1.37.0
plotly==5.22.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
pycountry==24.6.1
